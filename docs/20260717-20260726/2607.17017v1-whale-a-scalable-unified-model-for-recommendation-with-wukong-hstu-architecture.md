---
title: "WHALE: A Scalable Unified Model for Recommendation with Wukong-HSTU Architecture"
title_zh: "WHALE: 一种基于Wukong-HSTU架构的可扩展统一推荐模型"
authors: "Renqin Cai, Dawei Sun, Yuanjun Yao, Zhiyong Wang, Velvin Fu, Maggie Zhuang, Yu Shi, Zhongnan Fang, Xuan Cao, Jing Qian, Rui Li"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17017v1"
tags: ["query:rec-sys"]
score: 8.0
evidence: 统一推荐模型，融合非序列和序列特征
tldr: 现有推荐架构分别建模非序列特征和序列特征，缺乏统一可扩展方案。本文提出WHALE，每层融合Wukong高阶交互与HSTU长序列建模，通过注意力机制使Wukong查询HSTU表示，实现渐进式信息交换。在工业数据上离线实验持续提升，线上带来正向增益且吞吐量损失小。该方法已被部署至生产系统，为两类信息的可扩展统一提供了实用范例。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17017v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1707, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2607.17017v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 767, \"height\": 944}, {\"url\": \"assets/figures/arxiv/2607.17017v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 719, \"height\": 507}, {\"url\": \"assets/figures/arxiv/2607.17017v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1803, \"height\": 520}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17017v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 848, \"height\": 299}, {\"url\": \"assets/tables/arxiv/2607.17017v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 725, \"height\": 259}]"
motivation: 现有可扩展推荐架构分别建模非序列和序列特征，缺乏兼顾两者的统一设计。
method: WHALE每层包含Wukong、HSTU和注意力融合模块，通过Wukong查询HSTU实现渐进式信息交换。
result: 在工业数据集上离线性能持续提升，线上获得正向增益，且吞吐量损失可接受。
conclusion: WHALE有效统一了非序列和序列特征建模，已在生产部署验证实用性。
---

## 摘要
随着可扩展性在推荐建模中变得日益重要，近期架构沿不同路径对两类主要的排序信号进行了建模：非序列特征（包括用户、物品、上下文和交叉特征）以及来自用户行为历史的序列特征。Wukong和HSTU已成为这些路径代表性可扩展骨干模型：Wukong扩展了高阶非序列特征交互建模，而HSTU则扩展了长用户行为序列建模。尽管两者具有互补优势，但结合这两种特征建模的实用架构仍未被充分探索。我们提出了WHALE，一种可扩展的统一推荐架构，在Wukong和HSTU之上联合建模非序列和序列特征。每个WHALE层包含一个Wukong模块、一个HSTU模块以及一个基于注意力的融合模块，其中Wukong派生的交互表示查询HSTU派生的行为表示。该设计使两个骨干网络在整个网络中保持活跃，并实现渐进式Wukong-HSTU交换，允许高阶特征交叉反复从长用户历史中检索细粒度证据。为使WHALE可用于工业部署，我们引入了定制的Triton内核以及其他模型-系统协同设计技术，以提高训练和推理效率。在大规模工业推荐数据上，WHALE在离线实验中取得了持续改进。此外，它在线上收益方面也取得了正向结果，仅以适度的服务吞吐量权衡为代价。该方法已部署于生产系统。总体而言，WHALE为如何将这两类信息在工业推荐模型中以可扩展方式统一提供了一个实用示例。

## Abstract
As scalability becomes increasingly important in recommendation modeling, recent architectures have advanced the modeling of two broad sources of ranking signals along separate paths: non-sequence features, including user, item, context, and cross features; and sequence features from user behavior histories. Wukong and HSTU have emerged as representative scalable backbones for these paths: Wukong scales high-order non-sequence feature-interaction modeling, while HSTU scales long user-behavior sequence modeling. Despite their complementary strengths, practical architectures that combine these two types of feature modeling remain underexplored. We present WHALE, a scalable unified recommendation architecture that jointly models non-sequence and sequence features on top of Wukong and HSTU. Each WHALE layer contains a Wukong module, an HSTU module, and an attention-based fusion module in which Wukong-derived interaction representations query HSTU-derived behavior representations. This design keeps both backbones active throughout the network and enables progressive Wukong-HSTU exchange, allowing high-order feature crosses to repeatedly retrieve fine-grained evidence from long user histories. To make WHALE practical for industrial deployment, we introduce customized Triton kernels and other model-systems co-design techniques to improve training and inference efficiency. On large-scale industrial recommendation data, WHALE achieves consistent gains in offline experiments. Additionally, it delivers positive online gains with a modest serving-throughput trade-off. The method has been deployed in production systems. Overall, WHALE provides a practical example of how these two sources of information can be scalably unified in an industrial recommendation model.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代工业推荐系统中，排序信号可分为两大类——非序列特征（用户画像、物品属性、上下文特征、显式交叉特征）和序列特征（用户行为历史序列）。已有可扩展骨干模型Wukong专门建模高阶非序列特征交互，HSTU专门建模长用户行为序列，但两者互补且独立发展，缺乏一个统一的可扩展架构来同时利用这两类信号。
- **研究背景**：浅层混合方式（如先用HSTU压缩序列为汇总嵌入，再喂给Wukong）限制了高阶交叉对细粒度历史证据的检索，因为历史一旦压缩就无法再动态选择。WHALE旨在通过每一层的渐进式交换，让Wukong与HSTU持续互动，实现候选/上下文相关的精细特征交叉与序列建模的深度融合。
- **整体含义**：提出首个将Wukong和HSTU这两个代表性可扩展骨干统一在单一模型中的架构，并在工业级部署中验证其有效性，为后续推荐系统的规模化统一设计提供了实用范例。

## 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：每个WHALE层包含三个模块——Wukong模块（处理非序列特征交互）、HSTU模块（处理行为序列）、注意力融合模块（用Wukong输出查询HSTU输出）。通过堆叠多层，实现渐进式跨分支信息交换。

**关键技术细节**：
- **输入层**：将类别型特征映射为嵌入，数值型特征通过MLP映射，融合成非序列表示 \(E_{ns} \in \mathbb{R}^{N \times D}\)；序列特征（物品ID+侧信息）映射为序列表示 \(E_s \in \mathbb{R}^{L \times D}\)。
- **Wukong模块**（式1）：对非序列表示 \(X_{ns}^{(\ell)}\) 应用因子分解机块（FMB）和线性压缩块（LCB），产生高阶交互表示 \(H_w^{(\ell)}\)。
- **HSTU模块**（式2）：对序列表示 \(X_s^{(\ell)}\) 应用相对注意力和SwiGLU等操作，产生序列感知表示 \(H_s^{(\ell)}\)。
- **融合模块**：
  - 交叉注意力（式4）：Wukong输出作为查询 \(Q\)，HSTU输出作为键 \(K\) 和值 \(V\)（论文中共享 \(K=V\)），计算注意力输出 \(A^{(\ell)}\)。
  - 融合MLP（式5）：将 \(A^{(\ell)}\) 与 \(H_w^{(\ell)}\) 拼接后投影，加残差。
  - SwiGLU FFN（式6）：对融合结果应用门控前馈网络，得到最终融合表示 \(H_f^{(\ell)}\)，作为下一层非序列输入；同时HSTU输出直接传递为下一层序列输入。
- **系统优化**：
  - 定制Triton注意力内核：共享KV减少显存访问，异步反向传播变体（KV并行和Q并行）选择。
  - 共享门控SwiGLU（式8）：门控和上投影矩阵绑定，减少一次矩阵乘法。
  - 混合精度训练（BF16骨干+FP32损失等）和torch.compile编译器优化。
  - 推理优化：AOTInductor预编译、形状提示张量避免GPU-CPU同步。

## 3. 实验设计

- **数据集/场景**：使用某大型短视频社交平台的80B样本训练、4B样本评估；在线A/B测试在真实推荐流量上运行14天。
- **基准方法**：
  - Wukong-only ranker（仅非序列特征交互）
  - HSTU-only ranker（仅序列建模）
- **对比设置**：对齐三个模型的隐藏维度、层数、序列长度，以FLOPs衡量模型复杂度，在8、14、32 GFLOPs三个尺度上比较。
- **评价指标**：
  - 离线：归一化熵（NE），越低越好；该平台0.05% NE提升被视为明显胜出。
  - 在线：主要评价指标、Metric1、Metric2；0.03%胜出视为显著。
- **消融实验**：
  - 架构级：对比浅层混合（仅顶层融合）与WHALE的逐层渐进融合。
  - 模块级：将注意力替换为平均池化、去除融合MLP、去除SwiGLU FFN，分别测量NE回归。

## 4. 资源与算力

- **GPU型号**：NVIDIA B200 GPU。
- **训练细节**：全局batch size和优化器设置在不同运行中保持一致，每个模型训练一个epoch。
- **缺失信息**：论文未明确说明使用的GPU数量、节点数、总训练时长或内存配置。仅提及训练QPS优化提升30%，但未给出绝对数值。

## 5. 实验数量与充分性

- **实验组数**：
  - 架构对比：3种复杂度×3个模型 = 9个实验。
  - 缩放分析：序列长度4种配置、模型深度4种、模型宽度4种，共12个实验。
  - 消融实验：1个架构级 + 3个模块级对比。
  - 在线A/B测试：1组（14天）。
- **充分性与公平性**：
  - 离线实验设计了多维度缩放，覆盖长度、深度、宽度，验证了WHALE的扩展性。
  - 消融实验清晰展示了每部分贡献，但仅针对融合模块，未消融Wukong/HSTU内部变体。
  - 与基线的FLOPs对齐保证了比较公平。
  - 不足：仅在一个平台数据集上评估，未使用公开基准；未与最新混合架构（如Kunlun、OneTrans）直接对比（仅在相关工作提及差异）。实验结果未提供方差或置信区间。

## 6. 论文的主要结论与发现

- WHALE在所有三种FLOPs尺度下均优于Wukong-only和HSTU-only骨干，且优势随复杂度增加而扩大（14 GFLOPs时NE增益0.83% vs 32 GFLOPs时1.39%）。
- 缩放分析显示，增加序列长度（3k→15k）、模型深度（2→8层）、模型宽度（64→512维）均持续带来NE提升，无明显饱和趋势。
- 渐进式逐层融合显著优于浅层混合（NE回归0.25%），注意力机制是关键（替换为平均池化导致0.23%回归）。
- 在线A/B测试中，主要评价指标提升0.113%，Metric1 +0.824%，Metric2 +1.820%，推理QPS仅下降5%，在可接受范围内。
- 系统优化使训练吞吐量提高30%，推理吞吐量提高22%。

## 7. 优点

- **架构创新**：首次在每一层将Wukong和HSTU保持活跃并通过注意力融合，实现渐进式交互，而非简单的后期拼接或压缩。
- **工业实用性**：考虑了实际部署约束，开发了定制Triton内核、共享门控SwiGLU、混合精度、编译器优化等系统技术，使模型可在线上运行且吞吐量损失可控。
- **实验全面**：不仅离线缩放分析覆盖多个维度，还进行了在线A/B测试，并消融了融合模块的各个组件，结论有支撑。
- **可扩展性**：在序列长度、深度、宽度三个方向上均表现出正缩放趋势，表明架构能有效利用更大容量和更长历史。

## 8. 不足与局限

- **数据集单一**：仅使用一个短视频平台的数据，未在公开数据集或不同推荐场景（如电商、新闻）上验证，泛化性存疑。
- **对比基线不够全面**：未与近期其他统一架构（如Kunlun、OneTrans、UniMixer）进行直接实验比较，仅从设计原理上讨论差异。
- **消融实验覆盖有限**：仅消融了融合模块内部，未消融Wukong模块或HSTU模块的不同参数（如层数、注意力头数）对最终效果的影响。
- **计算资源细节缺失**：未报告训练所需GPU数、训练时长、内存占用等关键信息，不利于复现和成本估算。
- **潜在偏差风险**：论文中提到“Empirically, we observe this change does not degrade model quality”等语句，但未提供量化证据或统计检验。
- **在线实验仅一组**：未报告在线指标的置信区间或多次实验的稳定性，且仅在一个推荐表面测试，可能无法代表所有流量类型。

（完）
