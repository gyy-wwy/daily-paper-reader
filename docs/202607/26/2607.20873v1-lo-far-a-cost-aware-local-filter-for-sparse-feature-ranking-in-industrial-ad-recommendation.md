---
title: "LO-FAR: A Cost-Aware Local Filter for Sparse Feature Ranking in Industrial Ad Recommendation"
title_zh: LO-FAR：工业广告推荐中稀疏特征排序的成本感知局部过滤器
authors: "Egemen Erbayat, Luis Duque, Sohini Roychowdhury, Mohammad Amin, Srihari Reddy"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20873v1"
tags: ["query:rec-sys"]
score: 8.0
evidence: 面向工业广告推荐的成本感知稀疏特征排序
tldr: 工业广告推荐依赖稀疏高维ID特征，其嵌入表主导存储与计算成本，传统特征排序需GPU重训练，迭代缓慢。本文提出LO-FAR，一种仅CPU、模型无关的局部过滤框架，通过轻量估计器独立评估每个特征的预测信号，替代昂贵的交互感知方法。在百万级交互数据集上，LO-FAR在约2 CPU小时内完成475个特征排序，其下游CTR和CVR任务归一化熵增益与shuffle重要性等方法相当。该工作表明，在成本与周转约束下，简单局部过滤器是比复杂方法更实用的生产选择。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20873v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1843, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20873v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 445, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.20873v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1794, \"height\": 434, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20873v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 673, \"height\": 205, \"label\": \"Table\"}]"
motivation: 工业推荐中稀疏高基特征成本高昂，需在有限预算下高效排序以降低存储与计算开销。
method: 提出仅CPU运行的LO-FAR，用轻量局部估计器独立评估每个特征的保持预测信号，避免GPU重训练。
result: 在生产数据集上2 CPU小时完成475特征排序，下游任务增益与基于shuffle或随机门的方法竞争。
conclusion: 在成本和迭代时间约束下，简单局部过滤器可作为比交互感知方法更实用的生产方案。
---

## 摘要
工业广告推荐模型严重依赖稀疏、高基数的ID列表特征，这些特征编码用户历史和上下文标识符。每个特征都由一个专用的嵌入表支持，因此这些特征主导了存储、训练和服务成本，并且随着流量和下游模型的演变必须重新审视。因此，稀疏特征排序不仅仅是一个离线建模问题，也是一个受计算预算和迭代节奏限制的重复性系统决策。我们提出了局部特征排序（LO-FAR），这是一个仅使用CPU、模型无关的工作流程，通过轻量级局部估计器而非基于排列和随机门方法的GPU密集型重训练循环，从每个候选特征独立的留出预测信号中对其进行排序。在一个包含超过一百万条日志交互和475个稀疏ID列表特征的生产数据集上，LO-FAR在大约两个CPU小时内完成排序，并在CTR和CVR任务上保留了与基于洗牌的重要性、二元随机神经元以及基于覆盖的启发式方法相竞争的下游归一化熵增益，在保留100到400个特征的预算范围内。贡献在于提供了一个可部署的工作流程，表明当成本和周转时间约束严格时，简单的局部过滤器可以成为比更复杂的交互感知替代方案更实用的生产选择。

## Abstract
Industrial ad recommendation models rely heavily on sparse, high-cardinality ID-list features that encode user histories and contextual identifiers. Each is backed by a dedicated embedding table, so these features dominate storage, training, and serving cost and must be revisited as traffic and downstream models evolve. Therefore, sparse feature ranking is not just an offline modeling problem but also a recurrent systems decision limited by compute budgets and iteration cadence. We present Localized Feature Ranking (LO-FAR), a CPU-only, model-agnostic workflow that ranks each candidate feature from its stand-alone held-out predictive signal using lightweight local estimators rather than the GPU-bound retraining loops of permutation- and stochastic-gate-based methods. On a production dataset of more than one million logged interactions and 475 sparse ID-list features, LO-FAR completes ranking in approximately two CPU-hours and preserves downstream Normalized Entropy gains on CTR and CVR tasks that are competitive with shuffle-based importance, Binary Stochastic Neurons, and a coverage-based heuristic across budgets of 100--400 retained features. The contribution is a deployable workflow showing that, when cost and turnaround constraints are binding, a simple local filter can be a practical production choice over heavier interaction-aware alternatives.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：工业广告推荐系统中的稀疏ID特征（如用户历史、广告ID）由海量嵌入表支持，占参数量超过97%，主导存储、训练和服务成本。随着流量和下游模型变化，特征排名需要频繁重新评估，但传统方法（如排列重要性、随机门）依赖GPU重训练，成本高、迭代慢，无法满足生产环境频繁更新的需求。
- **研究动机**：将稀疏特征排名视为一个**重复性的系统决策问题**，需同时考虑下游质量、周转时间和操作简易性。提出一种低成本、可重复、仅CPU运行的第一阶段过滤方法，以替代或补充昂贵的交互感知方法。
- **整体含义**：在成本和周转约束严格时，简单的局部过滤器可成为实用的生产选择，而非追求离线准确性的复杂方法。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将每个候选稀疏特征的排名从下游模型训练中解耦，通过轻量级局部估计器独立评估其**留出预测信号**（stand-alone held-out predictive signal），实现CPU并行处理。
- **关键技术细节**（六阶段工作流）：
  1. **采样**：从日志数据中抽取代表性子集（约10^6行），按标签分层采样。
  2. **训练/测试划分**：划分出D_train和D_test，分层保持正样本率。
  3. **特征展开**：将每个特征的变长ID列表展开为每行一个ID，标签复制，得到展开数据集。
  4. **局部估计器**：对每个ID，若在训练集中出现≥K次，则估计其经验正样本率；否则使用K近邻回退（基于频率加权token距离）。
  5. **聚合**：对每个样本，取所有ID预测分数的均值作为特征级预测（实验表明均值、中位数、最大值效果相近）。
  6. **评分与排序**：在测试集上计算特征级预测的对数损失作为主要排序指标，辅助以平均精度和AUC。
- **复杂度**：O(p·n·ℓ·log(nℓ))，其中p为特征数，n为样本数，ℓ为平均列表长度。
- **执行模型**：仅CPU，特征间完全并行，可通过w个CPU worker线性加速。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **数据集**：来自大型工业广告推荐系统的生产级日志，包含超过一百万条用户-广告交互，共475个候选短ID列表特征（99分位列表长度小于5）。数据跨越较长的观察窗口以覆盖代表性变化。
- **场景**：CTR（点击率）和CVR（转化率）预测任务。
- **基准模型**：密集特征仅参考模型（不含任何稀疏特征），用于计算归一化熵（NE）增益。
- **对比方法**：
  - **Naive（覆盖度启发式）**：按特征覆盖率排序，成本O(p)。
  - **Shuffle-based importance**：基于排列重要性的模型无关方法，需多次重置或重训练。
  - **Binary Stochastic Neurons (BSN)**：基于L0正则化随机门的训练耦合特征选择方法，交互感知。
  - **LO-FAR**：本文方法。
- **评估协议**：对每个方法、每个预算B∈{100,200,300,400}，选取top-B特征后重新训练下游排名器，报告NE增益（相对于密集仅模型）。

### 4. 资源与算力
- **LO-FAR**：约2 CPU小时（并行化后）。
- **Naive**：分钟级（CPU）。
- **Shuffle-based importance**：多个GPU天（含多次重训/调参）。
- **BSN**：多个GPU天。
- **计算成本估算**（环境中观察）：LO-FAR低于100美元；Shuffle和BSN各自超过4000美元。
- **存储影响**：保留100–300个特征可减少约40–75%的稀疏嵌入表，对应删减约79%–16%的稀疏参数。
- **结论**：LO-FAR显著降低了算力需求，适合高频迭代。

### 5. 实验数量与充分性
- **实验数量**：共4种排名方法 × 4个预算 × 2个任务（CTR/CVR）= 32组主实验；另附Jaccard相似度分析（配对比较）。
- **充分性**：实验覆盖了从激进（100）到保守（400）的预算，并在两个任务上均验证了竞争性。提供了操作对比表（表2）和排名重叠图（图2）作为诊断。
- **客观性**：使用生产数据集，评估指标NE是工业标准；与多种代表性基线对比，包括简单启发式和复杂交互感知方法。但注意本文为工业论文，可能偏向于展示自身优势；且超参数敏感性分析简略（仅提及K和聚合函数影响不大），缺少全面消融实验。

### 6. 论文的主要结论与发现
- **下游质量**：在CTR和CVR任务上，LO-FAR在所有预算下均达到与Shuffle和BSN竞争的NE增益，特别是在B=200时质量-成本折中最佳。
- **迭代速度**：LO-FAR在约2 CPU小时内完成排名，远快于GPU多天的方法，支持日常迭代。
- **操作优势**：特征独立处理简化了并行、缓存、排错和监控，可集成到现有数据管道；不需要加速器，降低对GPU资源的竞争。
- **存储节省**：在保留质量的同时实现40–75%的稀疏嵌入表减少。
- **适用阶段**：LO-FAR适合作为第一阶段过滤器，先移除大量低信号特征，再对缩减后的池应用交互感知方法。

### 7. 优点
- **成本效率**：仅需CPU，计算成本极低，适合高频再排名。
- **模型无关性**：不依赖下游模型架构，可直接应用于不同排名器。
- **工程友好**：特征级并行、易缓存、易重试、易监控，与现有数据处理基础设施兼容。
- **操作清晰**：输出为排名，可被下游训练流水线直接使用。
- **结果实用**：在实际生产数据集上验证了竞争性，提供了明确的折中（质量 vs 成本）。

### 8. 不足与局限
- **交互依赖缺失**：对仅通过特征交互才提供信号的“交互专用”特征可能给出低分，导致在独立评估中丢失。作者承认应作为第一阶段，后续需辅以交互感知方法。
- **列表长度限制**：当前最佳适用于短ID列表特征（99分位长度<5），对长列表需要额外近似或序列感知估计器，文中未充分验证。
- **超参数敏感性未充分探讨**：仅简短提及K与聚合函数影响小，但缺乏系统消融实验。
- **评估范围有限**：仅在一个工业数据集上测试，且特征池为475个，未在更多数据集或更大规模上验证泛化性。
- **排名相似度诊断**：尽管作者强调NE增益是主要标准，但缺乏对冗余特征处理的深入分析；Jaccard相似度仅作为诊断，未讨论不同排序下模型性能差异的深层原因。
- **生产落地限制**：结果来自特定环境（Meta），绝对时间和成本可能因硬件、调度等而异。

（完）
