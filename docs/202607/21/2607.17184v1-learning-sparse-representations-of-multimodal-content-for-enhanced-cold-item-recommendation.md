---
title: Learning Sparse Representations of Multimodal Content for Enhanced Cold Item Recommendation
title_zh: 学习多模态内容的稀疏表示以增强冷物品推荐
authors: "Gregor Meehan, Johan Pauwels"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17184v1"
tags: ["query:rec-sys"]
score: 9.0
evidence: 通过多模态内容的稀疏表示解决冷启动问题
tldr: 推荐系统面临目录规模爆炸和新物品冷启动挑战。现有方法用多模态内容生成稠密嵌入但存储检索效率低。本文认为稀疏嵌入优势明显，提出预稀疏化激活技术源于线性注意力诱导锐度与去噪，并修改训练目标强制稀疏性。实验在四个多模态数据集上进行，结果显示稀疏嵌入在准确率上显著超过稠密基线，存储占用降低数倍，尤其在用户兴趣复杂时效果更佳。此外，稀疏嵌入具有自然可解释性，每个稀疏维度对应特定特征，并在准确率与存储权衡上更鲁棒。该方法为工业级冷启动推荐提供了高效解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17184v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1036, \"height\": 243, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 305, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 792, \"height\": 240, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1834, \"height\": 299, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 874, \"height\": 208, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1833, \"height\": 349, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 867, \"height\": 273, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 876, \"height\": 268, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 870, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17184v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 866, \"height\": 261, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17184v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 905, \"height\": 360, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17184v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1852, \"height\": 413, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17184v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 891, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17184v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 901, \"height\": 193, \"label\": \"Table\"}]"
motivation: 现代推荐系统中，大规模物品目录的嵌入存储与新物品冷启动问题亟需更高效的表示方法。
method: 提出学习稀疏多模态嵌入，通过预稀疏化激活技术（源于线性注意力）诱导锐度与去噪，并修改冷启动训练目标强制稀疏性。
result: 在四个多模态数据集上，稀疏嵌入在冷启动准确率上显著优于稠密嵌入，存储占用降低数倍，对多兴趣用户提升尤甚。
conclusion: 稀疏嵌入不仅降低存储成本、提升冷启动推荐精度，还具备可解释性和鲁棒性，适合工业部署。
---

## 摘要
现代数字平台中物品目录的规模与快速增长给推荐系统从业者带来了重大挑战。大多数推荐系统利用嵌入相似度来预测用户-物品偏好，但在工业级目录中，嵌入存储和低延迟检索非常困难。此外，新添加的物品没有对应的嵌入向量，无法有效推荐；以往的工作通常通过从辅助内容（如图像或描述性文本）生成冷物品表示来解决这一物品冷启动问题，从而无需历史交互即可预测用户偏好。在本文中，我们认为在这种基于内容的冷启动范式中，稀疏嵌入相比标准稠密向量具有显著优势。我们描述了如何将现有的冷启动训练方式适应于稀疏表示学习，并基于线性注意力的见解设计了一种预稀疏化激活技术，可在学习到的物品-物品相似度中引入尖锐性和去噪效果。我们证明，与稠密嵌入相比，所得的稀疏嵌入在显著降低存储成本的同时，尤其在具有多种兴趣的用户上，实现了冷启动推荐准确率的显著提升。通过在四个多模态推荐系统数据集上的综合实验，我们还展示了稀疏内容嵌入的可解释性及其在大小与准确率之间权衡的鲁棒性。

## Abstract
The scale and rapid growth of item catalogs in modern digital platforms present significant challenges to recommender system (RS) practitioners. Most RSs use embedding similarity to predict user-item preferences, but embedding storage and low-latency retrieval is challenging in industry-scale catalogs. Furthermore, newly added items do not have corresponding embeddings and cannot be recommended effectively; previous works often tackle this item cold-start problem by generating cold item representations from auxiliary content, such as images or descriptive text, so that user preferences can be predicted without historical interactions. In this paper, we argue that sparse embeddings have notable advantages over standard dense vectors in this content-based cold-start paradigm. We describe how existing cold-start training regimes can be adapted for sparse representation learning, and build on insights from linear attention to design a pre-sparsification activation technique that induces sharpness and denoising effects in learned item-item similarities. We show that the resulting sparse embeddings achieve significant improvements in cold-start recommendation accuracy over dense embeddings at considerably lower storage costs, especially for users with multiple interests. Through comprehensive experiments on four multimodal RS datasets, we also demonstrate the interpretability of sparse content embeddings and their robustness in the trade-off between size and accuracy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代推荐系统面临两个主要挑战：① 大规模物品目录导致稠密嵌入的存储和低延迟检索困难；② 新加入的冷物品没有交互历史，难以有效推荐。现有方法通过多模态内容（图像、文本等）生成稠密嵌入来解决冷启动，但稠密嵌入存在存储成本高、对多兴趣用户建模噪声大等缺陷。
- **整体含义**：论文提出利用稀疏表示来替代稠密嵌入，认为稀疏嵌入在基于内容的冷启动范式中具有显著优势——不仅能降低存储成本和检索延迟，还能通过其天然的稀疏性实现去噪，并借助预稀疏化激活技术提升相似度分布的尖锐性，从而提升冷启动推荐准确率，尤其对兴趣多样的用户效果更佳。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将内容相似度冷启动框架（SEMCo）的输出从稠密向量改为稀疏向量，通过 top-k 激活强制稀疏性，并借鉴线性注意力机制设计预稀疏化激活函数（PSAF）以增强嵌入的“尖峰”属性（sharpness）和去噪能力。
- **关键技术细节**：
  - **基础框架**：采用 SEMCo 的多模态内容投影网络 $f$，将多模态特征融合为单一表示 $\mathbf{y} \in \mathbb{R}^d$，然后进行 L2 归一化。用户偏好通过用户历史物品嵌入的聚合与冷物品嵌入的点积计算。
  - **稀疏化**：在投影网络输出后，应用 **top-k 激活**：仅保留绝对值最大的 $k$ 个维度，其余置零，然后 L2 归一化。典型设置 $k=32$，嵌入宽度 $d=1024$（或 2048 用于两侧操作）。
  - **预稀疏化激活函数（PSAF）**：在 top-k 之前，对 $\mathbf{y}$ 施加激活函数以诱导锐度和去噪。主要采用 **$\alpha$-entmax**（插值于 softmax 和 sparsemax 之间），并采用**两侧操作（two-side）**：$\text{two-side}(\mathbf{y}) = [\mathbf{y}; -\mathbf{y}]$，以保留负信号。整体 $\psi$ 函数为 $\psi(\mathbf{y}) = \alpha\text{-entmax}(\text{two-side}(\mathbf{y})/\omega)$，其中 $\omega$ 控制锐化强度。
  - **训练**：训练目标与原 SEMCo（对比学习）或 ELSA（重构）相同，不修改损失函数。采用指数剪枝策略，训练初期活跃维度从全宽逐渐衰减至 32，以避免死神经元。
  - **公式流程**：输入多模态特征 $\mathbf{x}_1,\dots,\mathbf{x}_M$ → 融合网络 $\mathbf{y}=f(\mathbf{x})$ → PSAF 和前处理 → top-k 激活 → L2 归一化 → 得到稀疏嵌入。用户偏好计算：$\mathbf{P}= (\mathbf{RY})\mathbf{C}^\top$，其中 $\mathbf{R}$ 是历史交互矩阵，$\mathbf{Y}$ 和 $\mathbf{C}$ 分别是温/冷物品稀疏嵌入矩阵。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：四个多模态推荐数据集：
  - Amazon 子集 **Clothing** 和 **Electronics**（图像+文本特征）
  - **Music4All-Onion (M4A-Onion)**（音频+歌词特征）
  - **Microlens**（微视频：图像+文本+视频特征）
- **基准方法**：
  - **稠密基线**：CLCRec、Heater、GAR、GoRec、ALDI（基于CF-内容对齐或生成）；以及稠密版 ELSA、SEMCo（softmax 和 sparsemax 变体）。
  - **稀疏方法**：稀疏版 ELSA、稀疏版 SEMCo（无 PSAF、或使用 sparsemax 等 PSAF）。
- **评估指标**：Recall@20 和 NDCG@20，在冷物品测试集上报告，所有结果基于 5 次运行的最优超参数。

### 4. 资源与算力

- 论文明确提到：所有实验在 **NVIDIA H100 GPU** 上运行。在最大数据集 Electronics 上，每 epoch 训练时间：
  - 稠密 1024d：12.8 秒
  - 稀疏 (单侧，无 PSAF)：13.7 秒
  - 稀疏 (两侧，sparsemax)：15.7 秒
  - GPU 内存占用：2.20~2.66 GB。
- 未提及使用多个 GPU 或总训练时长，但可推断单卡即可完成。

### 5. 实验数量与充分性

- **实验组数量**：
  - 主实验（表2）：4个数据集 × 每数据集约15种方法（包括稠密基线、稠密LAE、稀疏变体）。
  - 存储消融（图3）：4个数据集 × 多种存储大小。
  - 兴趣多样性分析（图4、5）：每个数据集按用户交互簇分组。
  - PSAF 消融（表3）：4种数据集 × 多种 PSAF（单侧/两侧，不同α值）。
  - 嵌入宽度实验（图7）：2个代表性数据集 × 多种宽度。
  - 用户稀疏化实验（图8）：2个数据集 × 多种维度组合。
  - 可解释性分析（图9）：M4A-Onion 的 PMI 分析。
- **充分性与客观性**：
  - 覆盖多个领域（电商、音乐、微视频），结论具有泛化性。
  - 消融实验系统，验证了 PSAF 和稀疏性的独立贡献。
  - 统计显著性检验（配对 t-test）表明大部分提升显著。
  - 开源代码，促进可复现性。

### 6. 论文的主要结论与发现

1. **稀疏嵌入在冷启动推荐中一致优于稠密嵌入**：相同存储预算下，NDCG@20 提升 16.6%~75.5%，Recall@20 提升 11.1%~57.1%。
2. **对多兴趣用户提升尤为显著**：用户兴趣越多样，稀疏嵌入相对于稠密基线的增益越大（图4）。
3. **PSAF（尤其是两侧 α-entmax）进一步增强性能**：通过提升相似度分布的尖峰性和去噪效果，在多数数据集上超越无 PSAF 的稀疏模型。
4. **稀疏嵌入具有可解释性**：每个活跃维度与特定语义标签（如音乐流派）高度关联（PMI 平均 3.81），且维度之间解耦良好，便于用户兴趣分析。
5. **鲁棒性**：性能对稀疏度（活跃维度数）和用户稀疏化不敏感，用户嵌入即使剪枝至与物品相同活跃度，准确率损失极小。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：
  - 首次将稀疏表示引入内容冷启动任务，并系统分析其优势。
  - 借鉴线性注意力中的核技巧，设计预稀疏化激活（PSAF），按需调谐相似度分布。
  - 两侧操作保留负信号，增加表达力而不增加推断存储。
  - 指数剪枝策略避免死神经元，训练稳定。
- **实验设计亮点**：
  - 存储成本严格对齐（稀疏存储算值+索引，与稠密等价比较）。
  - 深入分析多兴趣用户子群体，揭示稀疏嵌入优势来源。
  - 可解释性量化（PMI）和可视化，验证语义分离。
  - 对比基线丰富，包括最新生成式方法（ALDI, GAR）和内容LAE（ELSA, SEMCo）。

### 8. 不足与局限

- **实验覆盖局限**：
  - 仅在四个数据集上验证，缺少更多领域（如新闻、视频）的测试。
  - 未与大语言模型类冷启动方法（如ColdLLM）直接比较，因作者声明关注低延迟范式。
- **偏差风险**：
  - 用户兴趣簇的划分基于SEMCo嵌入的K-means，本身可能受表征质量影响。
  - 可解释性分析仅依赖M4A-Onion的流派标签，其他数据集无标签，未验证。
- **应用限制**：
  - 训练成本略有增加（约15-20%），但可接受；若嵌入宽度极大（>2048），PSAF的α-entmax计算可能成为瓶颈。
  - 稀疏嵌入的检索需要专门索引（如CSR或倒排索引），对某些工业系统可能增加工程复杂度。
  - Microlens 数据集上 PSAF（sparsemax）未提升甚至略降，说明该方法对交互结构（少用户、冷启动占比高）敏感，需谨慎选择。
- **其他不足**：
  - 未探讨缺失模态场景下的稀疏表示适应性。
  - 用户稀疏化虽鲁棒，但论文仅验证了保留全部活跃维度的情况，未测试更极端的剪枝比率。

（完）
