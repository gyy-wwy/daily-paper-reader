---
title: "SlimPer: Make Personalization Model Slim and Smart"
title_zh: SlimPer：让个性化模型轻量且智能
authors: "Siqi Wang, Xianjie Chen, Shaofeng Deng, Albert Chen, Romil Shah, Jiawei Huang, Zhaoqin Wang, Zhang Zhang, Yiqun Liu, Meilei Jiang, Anish Dubey, Moyan Mei, Tongxin Wang, Nathan Berrebbi, Misael Manjarres, Armand Sauzay, Shardul Kothapalli, Aryaman Vinchhi, Kevin Johnstone, Juheon Lee, Gufan Yin, Ziheng Huang, Justin Lin, Mert Terzihan, Yilin Qi, Cynthia Yang, Colin Peppler, Qi Ding, Ruohan Sun, Ge Song, Litao Deng, Parichay Kapoor, Matt Ma, Huihui Cheng, Jiyuan Zhang, Yanli Zhao, Yiping Han, Fangqiu Han, Ning Yao, Arun Singh, Jordan Edwards, Zhengyu Su, Abhishek Kumar, Guangdeng Liao, Ankit Asthana"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12281v1"
tags: ["query:rec-sys"]
score: 9.0
evidence: 将个性化排序重构成迭代细化过程以实现高效推荐
tldr: "Transformer架构在推荐系统中存在设计不匹配，其逐token生成模式导致中间张量随序列长度增长，而推荐仅需单分数。SlimPer将个性化排序重构为对紧凑<用户,物品>知识库的迭代细化，每层以O(N)成本选择性查询用户token并计算相关性，固定中间表示解耦模型深度与历史长度。在Instagram Reels和Feed部署后，用户参与度显著提升，并有效建模10k+历史事件。该方法统一稀疏、稠密及序列特征，通过注意力机制提供可解释性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12281v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 862, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12281v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 745, \"height\": 387, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12281v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1559, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12281v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 843, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12281v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 766, \"height\": 885, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12281v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 752, \"height\": 430, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12281v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 508, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12281v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1849, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12281v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1548, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12281v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 607, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12281v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 890, \"height\": 358, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12281v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 642, \"height\": 293, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12281v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 590, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12281v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 676, \"height\": 202, \"label\": \"Table\"}]"
motivation: Transformer的逐token设计不适合推荐系统的单分数输出，导致计算浪费，亟需高效个性化方法。
method: "SlimPer迭代细化<用户,物品>知识库，每层以O(N)成本选择性查询用户token，计算显式匹配分数。"
result: 在Instagram Reels和Feed部署，用户参与度提升，有效建模超10k用户历史事件。
conclusion: SlimPer解耦深度与序列长度，实现高效推荐，统一多模态特征并具备可解释性。
---

## 摘要
Transformer风格的架构越来越多地被用于工业推荐系统，但它们继承了一个与任务不匹配的设计前提：生成模型依赖于每个token的自回归预测，这证明了维持随序列长度扩展的大型中间张量是合理的。相比之下，推荐系统为每个<用户，物品>对产生一组单一的相关性分数，而无需token级别的监督。利用这一观察，我们提出了SlimPer，它将个性化排序重新表述为对紧凑、统一的<用户，物品>知识库进行迭代优化。在每一层，模型选择性地查询原始的多模态用户侧token，计算显式的相关性匹配分数，并优化知识库，所有操作都在固定大小的中间表示下，每层成本为O(N)。因此，模型深度与用户历史长度解耦，使得在不按比例增加计算或内存的情况下实现更深层次的相关性理解；仅请求优化通过在所有候选物品之间共享用户侧token的单一副本，进一步减少了内存。SlimPer在单一骨干网络中统一了稀疏、密集和序列特征，并通过其注意力机制提供了固有的可解释性。在Instagram Reels和Feed上部署后，SlimPer在简化整体系统的同时，实现了用户参与度的可测量提升，并能够有效建模超过1万个细粒度用户历史事件。

## Abstract
Transformer-style architectures are increasingly adopted for industrial recommendation systems, yet they inherit a design premise misaligned with the task: generative models rely on per-token autoregressive prediction, which justifies maintaining large intermediate tensors that scale with sequence length. In contrast, recommendation systems produce a single set of relevance scores for each <user, item> pair without token-level supervision. Leveraging this observation, we propose SlimPer, which reformulates personalized ranking as iterative refinement of a compact, unified <user, item> knowledge base. At each layer, the model selectively queries raw multi-modal user-side tokens, computes explicit relevance matching scores, and refines the knowledge base, all in O(N) per-layer cost with a fixed-size intermediate representation. As a result, model depth is decoupled from user history length, enabling deeper relevance understanding without proportional growth in compute or memory; request-only optimization further trims memory by sharing a single copy of user-side tokens across all candidate items. SlimPer unifies sparse, dense, and sequence features within a single backbone and provides inherent interpretability through its attention mechanism. Deployed on Instagram Reels and Feed, SlimPer yields measurable improvements in user engagement while streamlining the overall system and enabling effective modeling of 10k+ fine-grained user history events.

---

## 论文详细总结（自动生成）

# SlimPer 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **动机**：工业推荐系统广泛采用 Transformer 风格架构，但这类架构继承自生成式语言模型（如 LLM）的设计前提——逐 token 自回归预测，需要维护随序列长度 \(N\) 增长的巨大中间张量。然而推荐任务是判别式的，只需为每个 <用户, 物品> 对输出一组相关性分数，无需 token 级监督。因此 Transformer 在推荐中存在“设计不匹配”，导致不必要的计算和内存开销。
- **背景**：已有工作如 HSTU、OneTrans 等直接将 Transformer 用于推荐，面临两个困境：要么压缩中间表示导致信息损失，要么保留大张量导致成本随输入长度增长。当前工业系统通常将不同模态（稀疏、稠密、序列）用独立组件处理后再浅层拼接，系统复杂。
- **整体含义**：作者认为推荐的正确抽象不是序列建模，而是对紧凑的 <用户,物品> 知识库进行迭代细化，从而从根本上解耦模型深度与输入序列长度，实现更高效、更智能的个性化排序。

## 2. 提出的方法论：核心思想、技术细节与流程

### 核心思想
- **迭代细化范式**：将个性化排序重构为对固定大小的 <用户,物品> 知识库（Knowledge Base, KB）进行多轮迭代优化。每层执行三个步骤：选择（Select）、匹配（Match）、细化（Refine）。
- **关键优势**：模型深度（层数）与用户历史长度 \(N\) 解耦；每层都直接访问所有原始用户侧 token（无损信息）；计算复杂度仅为 \(O(N)\)（每层），中间表示大小固定（\(K \times d\)，通常 \(K=64\)）。
- **请求级优化（ROO-Aware）**：所有用户侧特征（稀疏、稠密、序列）在同一个请求内只计算一次，跨候选项共享，进一步减少冗余计算和内存。

### 技术细节

#### 输入与标记化（Tokenization）
- **稀疏特征**：通过 Embedding Pooling 得到固定大小向量。
- **用户历史事件**：每个事件融合内容（content）、动作类型（action type）、时间（temporal）和上下文（context）编码，得到 \(1 \times d\) 的 token。引入上下文编码（公式 1），利用事件附近窗口的邻居信息。
- **稠密特征**：拼接后经 MLP 投影为固定向量。

#### SlimPer 层（Select – Match – Refine）

**步骤1：选择（Select）——全模态感知的 QKV 注意力**
- 知识库 \(X_k \in \mathbb{R}^{K \times d}\) 通过线性投影得到查询 \(Q\)（公式 2）。
- 用户侧 token 分为两类：
  - 固定大小稀疏 token：使用 MLP 作为注意力核（公式 4，稀疏分支）。
  - 可变长序列 token：使用缩放点积注意力（公式 4，序列分支）。
- 输出注意力表示 \(R_s\) 和 \(R_e\)（公式 5-6），分别聚合来自稀疏和序列证据。

**步骤2：匹配（Match）——显式多面点积相关性匹配**
- 从知识库线性投影生成多面模板 \(T \in \mathbb{R}^{t \times d}\)（公式 7）。
- 计算模板与注意力输出的点积，得到匹配分数 \(\lambda_s, \lambda_e\)（公式 8-9）。

**步骤3：细化（Refine）——更新知识库**
- 将匹配分数、线性投影后的知识库、稠密特征拼接，经 RMSNorm 和 MLP 后残差更新知识库（公式 10）。
- 每层可输出任务嵌入（公式 11），多层求和后投影得到最终多任务 logits。

#### 迭代特性
- 堆叠多个 SlimPer 层（实验中 5~7 层），深层利用前层积累的证据逐步细化。
- 与 Transformer 的 \(O(N^2)\) 不同，SlimPer 每层复杂度 \(O(K \cdot N)\)，且中间内存固定为 \(O(K \cdot d)\)，不随 \(N\) 增长。

## 3. 实验设计

### 数据集与场景
- **数据集**：Instagram 的最终阶段排序模型所使用的内部训练和评估数据（Reels 和 Feed 两大排序面）。
- **基准（Baseline）**：Instagram 现有生产模型，采用**晚融合架构**：HSTU（假设序列特征）+ Wukong（假设固定大小特征）。这是当前最强的线上基线。

### 对比方法
- 主要对比：SlimPer vs. 上述基线。
- 未直接对比其他统一架构（如 OneTrans、HHFT、RankMixer），原因有二：一是混合架构（HSTU+Wukong）比单独统一 Transformer 更强；二是不同组织的训练数据、特征集、服务约束不同，直接数据不可比。

### 评估指标
- **离线指标**：归一化熵（Normalized Entropy, NE），与二分类交叉熵相关，越低越好。约 0.03% 的 NE 改进被认为有实际意义。
- **在线指标**：A/B 测试中的用户参与度指标（如转发、点赞、评论、关注、保存、跳过等）。
- **效率指标**：QPS（查询每秒）、内存使用（绝对变化百分比）。

## 4. 资源与算力

- **文中明确提及**：
  - 使用 Shampoo 优化器，学习率 0.05，训练一个 epoch。
  - SlimPer 的“训练和推理 GPU 容量大致中性”（A/B 测试结果）。
  - 未提供具体 GPU 型号、数量、训练时长。
- **备注**：由于是内部工业系统，论文未公开集群规模和计算账单。但 QPS 和内存的相对改善暗示计算需求低于基线。

## 5. 实验数量与充分性

- **实验组数**：
  - 离线对比：两个表面（Reels、Feed），各 6 个任务指标（表中列出 NE 相对变化），以及 QPS 和内存变化。
  - 缩放分析：图 4 展示了序列长度从 2k 到 6k 的 NE vs QPS 曲线（两个模型）。
  - 消融实验（Ablation）：
    - 知识库大小 \(K\)（5 种设置：4/8/16/32/64）+ 查询和模板大小按比例调整。
    - 层数 \(L\)（4 种：3/5/7/9）。
    - 上下文编码组件（无上下文/仅事件类型/全上下文）。
  - 可解释性分析：注意力热图定性分析（附录 E）。
  - FLOPs 估算：表 8 给出不同序列长度下的理论 FLOPs 对比。
  - A/B 测试：在 Reels 和 Feed 上全流量部署，统计数据显著。
- **充分性评价**：
  - **充分**：覆盖了主要性能对比、缩放规律、关键设计消融，以及线上大规模验证。
  - **公平**：基线为当前线上最强模型，控制相同训练数据和优化器设置。但未与统一架构直接对比是一个缺口，作者已给出合理解释。
  - **客观**：所有改进均以百分比形式相对于基线报告，且强调了 NE 改进的方向一致性。

## 6. 主要结论与发现

1. **离线性能**：SlimPer 在所有任务上持续优于基线（NE 改进：-0.23% 到 -1.05%，取决于任务和序列长度）。在相同序列长度下，同时获得 NE 提升和 QPS 提升（例如 Reels 2k：NE -0.51%，QPS +11%）。
2. **缩放优势**：随着序列长度增长，SlimPer 的 NE 改进幅度增大（例如 Feed 转发从 1k 的 -0.49% 到 4k 的 -0.94%），而效率损失更小（图 4 显示基线在 6k 时 QPS 下降 48%，SlimPer 仅下降 <20%）。FLOPs 分析表明 SlimPer 在 2k 时只需基线的约 1/8 计算量，在 6k 时约 1/25。
3. **内存效率**：在相同序列长度下，内存减少 9%~18%。来源：固定大小知识库取代 \(O(N)\) 中间张量，以及 ROO 共享用户侧 token。
4. **线上效果**：全流量 A/B 测试实现统计显著的用户参与度提升，整体影响约为典型显著发布效果的 10 倍；生态指标（完整性、多样性、新鲜度）无退化；GPU 容量中性。
5. **消融发现**：
   - 上下文编码主要获益来自事件类型编码（约 -0.15% NE），内容编码几乎无额外增益。
   - 知识库大小 \(K=64\) 是最佳权衡；\(K=4\) 导致 +1.2% NE 退化。
   - 7 层效果良好，9 层仍有收益（-0.12% NE）但成本增加。
6. **可解释性**：注意力热图显示低层关注历史全局，中层聚焦近期，高层进一步集中到最近约 400 个事件，验证了迭代细化的有效性。

## 7. 优点

- **关键创新**：提出可微分推荐的新范式——迭代细化知识库，直接解决 Transformer 在推荐中的“不匹配”问题。
- **架构简洁统一**：使用 MLP、QKV 注意力、线性投影、RMSNorm 等标准模块，统一处理稀疏、稠密、序列特征，替代了复杂的多组件拼接系统。
- **效率高**：固定大小中间表示解耦深度与 \(N\)，显式匹配步骤将计算导向用户-物品相关性，而非用户侧内部交互；ROO 进一步减少冗余。
- **可解释性强**：注意力机制提供自然归因，每层可追溯决策依据。
- **实践验证充分**：在 Instagram 两大主要排序面上线，取得了显著的实际业务收益，验证了方法的规模化和可靠性。

## 8. 不足与局限

- **缺乏与最新统一架构的直接对比**：论文主要对比混合基线（HSTU+Wukong），未与 OneTrans、HHFT、RankMixer 等统一架构在相同条件下对比。作者以“混合基线更强”为由辩解，但若能与统一架构对比会更完整。
- **不可完全复现**：所有实验基于内部 Instagram 数据，数据量远超公开基准，且训练到收敛。论文未提供代码或公开数据集。不过提供了详细超参数设置（附录 A 表 4），使实践者可在自己的推荐系统上复现思路。
- **计算资源细节不透明**：未说明 GPU 型号、数量、训练时长，使得客观比较成本时缺少依据。
- **上下文编码的边际收益**：添加内容上下文仅带来 <0.02% NE 改善，说明该部分设计并非核心贡献，可进一步简化。
- **泛化性边界**：方法针对最终阶段排序（小候选集）设计，作者展望扩展到早期阶段（大候选集）但未验证。另外，知识库大小 \(K\) 和层数 \(L\) 的设定依赖于经验，未提供自适应的选择策略。

（完）
