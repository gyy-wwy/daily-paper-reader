---
title: "TMallGS: Scaling Unified Feature and Sequence Modeling for Generative E-commerce Search"
title_zh: TMallGS：面向生成式电商搜索的统一特征与序列建模扩展
authors: "Zhentao Song, Yufeng Gao, Xing Fang, Jing Wang, Guangxin Song, Bokang Wang, Yipin Dai, He Guo"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13398v1"
tags: ["query:rec-sys"]
score: 9.0
evidence: 用于生成式电商搜索的统一特征与序列建模扩展
tldr: 针对电商搜索排序从传统DLRM向统一Transformer架构转型时特征异构性问题，提出TmallGS模型。采用分层分布校准分词、场自适应门控Transformer、解耦FiLM后期融合等五项创新组件。离线实验与在线A/B测试表明，TmallGS提升训练吞吐量，显著提高UCTCVR和GMV。该工作为工业级生成式搜索排序提供可扩展的统一特征与序列建模方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13398v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1775, \"height\": 1120, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13398v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 444, \"height\": 716, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13398v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 601, \"height\": 423, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13398v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 876, \"height\": 678, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13398v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 903, \"height\": 966, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13398v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 568, \"height\": 341, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13398v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 837, \"height\": 247, \"label\": \"Table\"}]"
motivation: 现有方法如OneTrans对所有特征统一分词，忽视异构性，需高效可扩展架构以适配电商搜索复杂特征。
method: 提出TmallGS，包含分层分布校准分词、场自适应门控Transformer、解耦FiLM后期融合、上下文感知偏置网络和误差感知渐进训练五个组件。
result: 在Tmall搜索离线实验与在线A/B测试中，训练吞吐量提升，UCTCVR和GMV显著增长。
conclusion: TmallGS为电商搜索排序提供了可扩展的Transformer架构，验证了统一特征与序列建模的有效性。
---

## 摘要
在工业搜索与排序系统中，点击率（CTR）预测正从传统的深度学习推荐模型（DLRM）转向统一的、计算密集型的Transformer架构。这一转变源于提升模型FLOPs利用率（MFU）并通过扩展定律实现可预测收益的需求。然而，现有方法如OneTrans和Climber在适配大语言模型（LLM）架构时，往往采用全词元化策略，忽略了排序特征的异构性。我们提出TMallGS，一种面向天猫搜索的可扩展排序架构。TMallGS包含五个关键组件：（1）分层分布校准词元化，结合域级显著性重加权（FSR）和分布校准投影（DCP），将多样化特征映射到优化子空间；（2）域自适应门控Transformer主干，具有逐域QKV投影和噪声自适应门控，以实现精细的语义交互；（3）解耦FiLM晚期融合，以保留显式高频信号；（4）上下文感知偏置网络，以解耦系统性偏置与用户意图；（5）误差感知渐进训练，采用动态加权损失以实现鲁棒学习。在天猫搜索上进行的大量离线实验和在线A/B测试表明，TMallGS提高了训练吞吐量，并在UCTCVR和GMV上取得了显著收益。

## 速览
**TLDR**：工业搜索排序系统从DLRM向统一Transformer架构演进，但现有方法因全token化策略忽略特征异质性，导致性能瓶颈。本文提出TmallGS，通过层次化分布校准tokenization、场自适应门控Transformer、解耦FiLM融合、上下文偏置网络和误差感知渐进训练，统一处理异构特征和序列建模。离线实验和在线A/B测试表明，TmallGS在提升训练吞吐量的同时，显著提高UCTCVR和GMV。该工作为电商搜索生成式排序提供了可扩展的基线方案。 \
**Motivation**：现有Transformer排序模型采用全token化策略，忽视特征异质性，无法适应工业场景的高效训练与扩展需求。 \
**Method**：提出TmallGS，包含五种组件：层次化分布校准tokenization、场自适应门控Transformer、解耦FiLM融合、上下文偏置网络和误差感知渐进训练。 \
**Result**：离线实验和在线A/B测试显示，TmallGS在淘宝搜索中提升了训练吞吐量，UCTCVR和GMV均获得显著增长。 \
**Conclusion**：TmallGS通过统一异构特征与序列建模，实现了工业级搜索排序的可扩展生成式框架。

---

## Abstract
In industrial search and ranking systems, Click-Through Rate (CTR) prediction is shifting from traditional Deep Learning Recommendation Models (DLRM) toward unified, compute-intensive Transformer architectures. This transition is driven by the need to improve Model FLOPs Utilization (MFU) and achieve predictable gains through scaling laws. However, existing approaches such as OneTrans and Climber often adopt an all-in-tokenization strategy when adapting Large Language Model (LLM) architectures, overlooking the heterogeneous nature of ranking features. We propose TmallGS, a scalable ranking architecture for Tmall search. TmallGS includes five key components: (1) Hierarchical Distribution-Calibrated Tokenization, which combines Field-wise Saliency Reweighting (FSR) and Distribution-Calibrated Projection (DCP) to map diverse features into optimized subspaces; (2) a Field-Adaptive Gated Transformer Backbone with per-field QKV projections and noise-adaptive gating for refined semantic interaction; (3) Decoupled FiLM Late Fusion to preserve explicit high-frequency signals; (4) a Context-Aware Bias Net to decouple systemic bias from user intent; and (5) Error-Aware Progressive Training with dynamically weighted losses for robust learning. Extensive offline experiments and online A/B tests on Tmall Search show that TmallGS improves training throughput and achieves substantial gains in UCTCVR and GMV.

---

## 论文详细总结（自动生成）

# TMallGS 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **问题背景**：工业搜索排序（CTR预测）正从传统的深度学习推荐模型（DLRM，如DeepFM、DCNv2）向统一、计算密集型的Transformer架构转型。DLRM存在内存访问瓶颈（低MFU）和规模收益递减问题，无法利用现代GPU的张量核心。
- **现有方法缺陷**：OneTrans、Climber等直接移植LLM架构时采用“全词元化”策略，忽略特征异构性，导致梯度冲突、信号稀释（显式交叉特征被低通滤波压制）和优化不稳定（稀疏二元反馈导致梯度消失）。
- **核心挑战**：搜索场景具有严格的查询约束，需要保留高频匹配信号，同时处理长短不一、语义差异巨大的异构特征（如查询文本、用户行为序列、物品属性、统计交叉特征）。
- **本文意义**：提出TMallGS，为天猫搜索排序提供可扩展、高性能的统一特征与序列建模方案，首次在工业级搜索场景中验证了Transformer架构的扩展定律收益。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：“语义分治、交互解耦”——将异构特征分层处理，将显式匹配信号与深层语义表示解耦，避免相互干扰。
- **整体架构**：包含五个协同组件，按三个阶段处理流：
  - **阶段A：分层分布校准词元化**
    - **域级显著性重加权（FSR）**：对每个特征域聚合全局上下文（沿时间和特征维度平均），通过轻量级瓶颈网络生成域级权重，对输入域进行软选择。
    - **分布校准投影（DCP）**：使用校准Swish激活（自门控+层归一化）进行双线性投影，相比SwiGLU-FFN减少约33% FLOPs。
  - **阶段B：域自适应门控Transformer主干（TmallGS块）**
    - **逐域QKV投影**：每个特征域使用独立的Q/K/V投影矩阵，实现流形对齐，缓解梯度冲突。
    - **上下文主导可见性掩码**：序列分为上下文前缀（锚点、用户、查询、历史）和候选集；上下文内全可见，候选仅可见自身和上下文，候选之间不可见，防止信息泄露。
    - **噪声自适应门控注意力**：在标准注意力输出后，基于预层归一化输出生成逐元素门控，可动态抑制无关历史噪声（打破注意力总和为1的线性瓶颈）。
  - **阶段C：解耦FiLM后期融合**
    - 将候选的轻量语义嵌入（来自Transformer输出）与完整候选特征（含重交叉特征）拼接，通过MLP生成仿射变换系数（缩放因子γ和偏移β），对拼接向量进行特征线性调制（FiLM），恢复高频显式信号。
  - **上下文感知偏置网络**：利用上下文锚点token的深层编码（已充分聚合请求级全局信息）直接生成偏置logit，与主语义logit加性融合。偏置在计算pairwise loss时自然抵消，使排序只关注相对相关性。
  - **误差感知渐进训练**：每层轻量辅助头输出中间预测，计算绝对误差作为下一层损失权重（停止梯度），实现自适应困难样本挖掘。总损失为渐进点wise BCE损失与最终pairwise排序损失的加权和。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：天猫APP搜索日志，31天，超过5亿样本，平均用户行为序列长度1500，用户数1300万，物品数7400万。训练集（前30天）4.84亿，测试集（最后1天）1600万。无公开大规模搜索数据集能满足长序列和显式匹配信号需求，因此仅使用工业数据集。
- **基准（Baseline）**：分为两组：
  - 传统模型：DNN、DIN（生产基线）、DCNv2、APG。
  - 可扩展架构：HiFormer、RankMixer（生产基线）、HHFT、HSTU、MTGR、OneTrans。
- **评估指标**：AUC和GAUC（按用户-查询会话内加权平均AUC，工业上GAUC提升0.001视为显著）。
- **实验设置**：全局batch size 4096，学习率2e-4，使用拆分优化器（稀疏用Adam，稠密用AdamW）。所有DLRM用相同MLP维度，所有Transformer用相同隐藏维度。

## 4. 资源与算力

- **明确说明**：论文中提到“在16块高性能Nvidia GPU集群上训练”（Section 5.1.4），未具体说明GPU型号、显存、训练时长。正文也无单次训练周期、总参数量（除在线配置提到约0.5亿参数）等详细算力消耗。
- **补充信息**：在线推理配置为4层、1024维度、8头、序列长度1500，稠密主干约0.05B参数，推理延迟增加6ms。但完整训练阶段的FLOPS、GPU hours等未给出。

## 5. 实验数量与充分性

- **主要实验组**：
  - 整体性能对比（Table 2）：9个基线 + TmallGS，共10个模型在单一数据集上对比。
  - 消融实验（Table 3）：6组变体（移除FiLM、替换噪声门控、简化分词、移除偏置网络、移除渐进训练、移除pairwise loss、移除查询历史），每组报告AUC和GAUC变化。
  - 扩展性实验（Figure 2）：沿宽度、深度、长度三个维度进行缩放曲线分析。
  - 热身策略对比（Table 5）：冷启动 vs 直接加载 vs 两阶段热身。
  - 在线A/B测试（Table 4）：30天在线结果，报告PV-AUC、Imp-GAUC、UCTCVR、GMV和延迟变化。
- **充分性评估**：实验较为充分，覆盖了效果验证、消融、可扩展性、部署策略。但仅使用单一工业数据集（天猫搜索），缺乏公开数据集对比（论文解释因公开集不满足长序列和显式交叉信号要求）。在线实验为期30天，统计显著性p<0.05。消融实验只报告了AUC/GAUC变化，未报告统计显著性或方差。

## 6. 主要结论与发现

- **核心结论**：TMallGS在离线测试上比生产基线（DIN+RankMixer）高出GAUC +1.26%/+1.12% AUC；在线A/B测试中，UCTCVR提升1.38%，GMV提升1.52%，延迟增加6ms（SLA内）。
- **可扩展性**：验证了在宽度、深度、序列长度三个维度上可遵循扩展定律（单调增益），且深度扩展效益最高，长度扩展至1500无饱和（得益于噪声自适应门控）。
- **关键领悟**：计算密集型架构（Group 2）普遍优于内存密集型DLRM（Group 1）；解耦设计（保留高频信号）是LLM架构适配搜索的关键。
- **热身策略**：两阶段热身（先冻结稀疏嵌入训练稠密主干，再联合微调）比直接加载获得+1.42% AUC提升。

## 7. 优点：方法或实验设计亮点

- **方法创新**：系统性地解决特征异构性（逐域投影、分布校准）、信号稀释（FiLM解耦融合）、噪声序列（自适应门控）、偏置解耦（上下文偏置网络）、训练不稳定（渐进加权）五大问题，具有高度针对性。
- **工程实用性**：所有设计均考虑工业部署（低延迟、高吞吐），FSR+DCP相比SwiGLU-FFN节省约33% FLOPs；在线配置仅增加6ms延迟即可带来显著业务指标提升。
- **实验设计**：涵盖离线、消融、扩展性、热身策略和在线A/B测试，在线测试为期30天，统计显著性明确，结果可信度高。
- **消融实验细致**：区分了有无重交叉特征的场景，验证了FiLM在两种场景下的优势，具有深入性。
- **可扩展性分析**：采用统一计算预算（GFLOPs）进行公平比较，避免参数量不等带来的偏差。

## 8. 不足与局限

- **数据集单一**：仅在天猫搜索数据集上实验，缺乏对其他电商平台或非搜索场景（如推荐流）的泛化验证。虽然解释了原因，但外部有效性受限。
- **消融实验未报告方差或置信区间**：只报告了相对变化百分比，未展示多次重复实验的波动，无法判断差异的统计显著性。
- **算力细节缺失**：未公开训练时长、GPU型号、总FLOPS等，不利于复现和能耗评估。
- **延迟影响分析较简略**：在线环境仅报告平均延迟增加6ms，未讨论尾部延迟、QPS变化、资源扩展性等。
- **与最新生成式检索范式对比不足**：虽然列出了MTGR、OneRec等，但未详细讨论生成式方法在搜索排序中的可控性问题，也未在相同实验条件下对比黑盒生成式模型的具体量化结果。
- **扩展性曲线未展示上限**：虽然长度扩展至1500无饱和，但未测试更长序列（如5000+）的行为，也未分析计算资源随序列长度超线性增长的代价。

（完）
